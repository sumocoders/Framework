{% for flashMessage in app.session.flashbag.get('success') %}
    <div class="alert alert-success alert-dismissible notification" role="status">
        <div class="container">
            <a class="close" data-dismiss="alert" title="{{ 'dialogs.close'|trans|capitalize }}">
                <i class="fa fa-close"></i>
                <span class="hide">{{ 'dialogs.close'|trans|capitalize }}</span>
            </a>
            {{ flashMessage }}
        </div>
    </div>
{% endfor %}

{% for flashMessage in app.session.flashbag.get('report') %}
    <div class="alert alert-success alert-dismissible notification" role="status">
        <div class="container">
            <a class="close" data-dismiss="alert" title="{{ 'dialogs.close'|trans|capitalize }}">
                <i class="fa fa-close"></i>
                <span class="hide">{{ 'dialogs.close'|trans|capitalize }}</span>
            </a>
            {{ flashMessage }}
        </div>
    </div>
{% endfor %}

{% for flashMessage in app.session.flashbag.get('warning') %}
    <div class="alert alert-warning alert-dismissible notification" role="alert">
        <div class="container">
            <a class="close" data-dismiss="alert" title="{{ 'dialogs.close'|trans|capitalize }}">
                <i class="fa fa-close"></i>
                <span class="hide">{{ 'dialogs.close'|trans|capitalize }}</span>
            </a>
            {{ flashMessage }}
        </div>
    </div>
{% endfor %}

{% for flashMessage in app.session.flashbag.get('error') %}
    <div class="alert alert-danger alert-dismissible notification" role="alert">
        <div class="container">
            <a class="close" data-dismiss="alert" title="{{ 'dialogs.close'|trans|capitalize }}">
                <i class="fa fa-close"></i>
                <span class="hide">{{ 'dialogs.close'|trans|capitalize }}</span>
            </a>
            {{ flashMessage }}
        </div>
    </div>
{% endfor %}

{# @todo    fix me
{option:formHasError}
<div class="alert alert-error" role="alert">
    <div class="container">
        <button type="button" class="close" data-dismiss="alert" title="{{ 'dialogs.close'|trans|capitalize }}">{{ 'dialogs.close'|trans|capitalize }}</button>
        {$errGeneralFormError}
    </div>
</div>
{/option:formHasError}
#}
